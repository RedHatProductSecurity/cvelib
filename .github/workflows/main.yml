name: Run CI

on: [push, pull_request]

jobs:
  tox:
    name: Run Tox
    steps:
    - uses: actions/checkout@v2
    - name: Run all envs
      uses: fedora-python/tox-github-action@master
      with:
        tox_env: ${{ matrix.tox_env }}
    strategy:
      matrix:
        tox_env: [cvelib, black, isort, flake8, py36, py37, py38, py39, py310]

    runs-on: ubuntu-latest
